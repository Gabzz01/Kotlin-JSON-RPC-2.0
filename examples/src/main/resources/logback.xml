<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <!-- Targeting System.err to not corrupt STDIO example -->
        <target>System.err</target>
        <encoder>
            <pattern>
                %highlight(%-5level) %d{"HH:mm:ss"} %yellow(%-30.30logger{0}) | %msg%n
            </pattern>
        </encoder>
    </appender>
    <root level="INFO">
        <appender-ref ref="console"/>
    </root>
</configuration>